app MultiStateAlarm initial state off {
    bricks 
         Actuator led : 13
         Actuator buzzer : 8
         Sensor button : 10
     states 
         off {
             led <= LOW
             buzzer <= LOW
             if(button is HIGH) => buzz_on
         } 
         buzz_on {
             led <= LOW
             buzzer <= HIGH
             if(button is HIGH) => led_on
     }
     led_on{
     		led <= HIGH
             buzzer <= LOW
             if(button is HIGH) => off
     }
}