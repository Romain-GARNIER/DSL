app SimpleAlarm initial state off {
    bricks 
         Actuator digital led : 8
         Sensor analog button : 10
         
     states 
         off {
             led <= LOW
             if (button > 500) => on
         } 
         on {
             led <= HIGH
             if (button < 30) => off
     }
}